# @sensolus packages are published to the public npm registry (npmjs.com).
@sensolus:registry=https://registry.npmjs.org/

# NOTE: no _authToken line here on purpose — a project .npmrc overrides your
# `npm login` credentials in ~/.npmrc. CI injects its token into ~/.npmrc
# (see Jenkinsfile); developers just use `npm login`.
